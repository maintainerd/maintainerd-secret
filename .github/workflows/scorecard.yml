# OpenSSF Scorecard — automated security health assessment
# Public results: https://scorecard.dev/viewer/?uri=github.com/maintainerd/maintainerd-secret
# Docs: https://github.com/ossf/scorecard-action

name: OpenSSF Scorecard

on:
  push:
    branches: [main]
  schedule:
    # Weekly scan — Monday 06:00 UTC
    - cron: "0 6 * * 1"
  # On-demand re-scan (e.g. right after signing a fresh release).
  workflow_dispatch:

permissions: read-all

jobs:
  scorecard:
    name: Scorecard
    runs-on: ubuntu-latest
    permissions:
      # Needed to upload SARIF results to GitHub Code Scanning
      security-events: write
      # Needed for the OIDC token used by Scorecard to publish results
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Run OpenSSF Scorecard
        uses: ossf/scorecard-action@4eaacf0543bb3f2c246792bd56e8cdeffafb205a # v2.4.3
        with:
          results_file: scorecard.sarif
          results_format: sarif
          # The default GITHUB_TOKEN can't read branch-protection settings, which makes
          # the Branch-Protection check error (-1). A PAT with repo/administration:read
          # fixes it. Create a secret named SCORECARD_TOKEN; falls back to github.token.
          repo_token: ${{ secrets.SCORECARD_TOKEN || github.token }}
          # Publish results so they appear on https://scorecard.dev
          publish_results: true

      - name: Upload Scorecard results to GitHub Code Scanning
        uses: github/codeql-action/upload-sarif@5595ccaf912efad79be6eef63a5619ff05969be3 # v4
        with:
          sarif_file: scorecard.sarif
          category: scorecard
