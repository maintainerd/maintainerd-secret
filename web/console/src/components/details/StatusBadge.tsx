// StatusBadge now lives in @/components/badges (the canonical badge module).
// Re-exported here for the existing detail-page call sites.
export { StatusBadge } from "@/components/badges/StatusBadge"
